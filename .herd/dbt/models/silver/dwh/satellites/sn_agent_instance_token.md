{% docs sn_agent_instance_token %}
Non-historized satellite for Agent instance token usage (append-only)

**Grain:** One row per event (append-only, non-historized)
**Source:** Transactional link
{% enddocs %}
